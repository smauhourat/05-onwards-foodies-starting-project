export default function NotFound({ error }) {
    return (
        <main className="not-found">
            <h1>Not found</h1>
            <p>Unfortunalely, we could not find the requested page or resource.</p>
        </main>
    )
}